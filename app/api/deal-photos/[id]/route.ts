import {env} from "cloudflare:workers";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params,photo=await env.DB.prepare("SELECT object_key,content_type FROM deal_photos WHERE id=?").bind(id).first<{object_key:string;content_type:string}>();if(!photo)return new Response("Not found",{status:404});const object=await env.BUCKET.get(photo.object_key);if(!object)return new Response("Not found",{status:404});return new Response(object.body,{headers:{"Content-Type":photo.content_type,"Cache-Control":"public, max-age=3600","X-Content-Type-Options":"nosniff"}})}
