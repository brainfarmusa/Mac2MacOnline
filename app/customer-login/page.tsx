"use client";
import {FormEvent,useState} from "react";
import {pddAuthFetch,type PddSession} from "../../lib/pdd-auth";
import {saveCustomerSession} from "../../lib/customer-auth";
export default function CustomerLogin(){const [mode,setMode]=useState<"signin"|"create">("signin");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(event:FormEvent){event.preventDefault();setBusy(true);setMessage("");try{const path=mode==="create"?"/auth/v1/signup":"/auth/v1/token?grant_type=password";const response=await pddAuthFetch(path,{method:"POST",body:JSON.stringify({email:email.trim().toLowerCase(),password})});const data=await response.json() as PddSession&{access_token?:string;error_description?:string;msg?:string};if(!response.ok)throw new Error(data.error_description||data.msg||"Unable to continue.");if(!data.access_token){setMessage("Check your email to confirm your new account, then return here to sign in.");setMode("signin");return}saveCustomerSession(data);window.location.assign("/my-bids")}catch(error){setMessage(error instanceof Error?error.message:"Unable to continue.")}finally{setBusy(false)}}return <main className="employeeAuthPage"><section className="employeeAuthCard"><a className="employeeAuthBrand" href="/"><img src="/assets/m2m-logo-large.png" alt="Mac2MacOnline"/></a><p className="eyebrow">CUSTOMER ACCOUNT</p><h1>{mode==="signin"?"Customer sign in":"Create your account"}</h1><p>See every bid you submit and whether it is pending, won, or lost.</p><form onSubmit={submit}><label>Email address<input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" autoComplete={mode==="create"?"new-password":"current-password"} minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="button" disabled={busy}>{busy?"Please wait…":mode==="signin"?"Sign in":"Create account"}</button></form><button className="employeeInviteLink" type="button" onClick={()=>{setMode(mode==="signin"?"create":"signin");setMessage("")}}>{mode==="signin"?"New customer? Create an account":"Already have an account? Sign in"}</button>{message&&<p className="employeeAuthMessage" role="status">{message}</p>}<a className="employeeBackLink" href="/public-deal-desk">← Continue as a visitor</a></section></main>}
