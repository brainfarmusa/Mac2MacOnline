"use client";
import {useEffect,useState} from "react";
import {clearPddSession,currentPddSession,deletePddDocument,downloadPddDocument,pddAuthFetch,replacePddDocument,type PddSession} from "../../../lib/pdd-auth";
import {createOrderPdfBlob,type OrderPdfLine} from "../../../lib/orderPdf";
import {createOrderSpreadsheetBlob} from "../../../lib/orderSpreadsheet";

type Vendor={company_name:string;contact_name:string;email:string;phone:string;address1:string;address2:string;city:string;region:string;postal_code:string;country:string};
type PurchaseOrder={id:string;po_number:string;deal_number:string;vendor_total:number;margin_percent:number;source_type?:string;source_file_name?:string;generated_by_name:string;created_at:string;xlsx_storage_path:string|null;pdf_storage_path:string|null;vendor:Vendor|null};
type SalesOrder={id:string;so_number:string;deal_number:string;customer_company:string;customer_contact:string;customer_email:string;customer_phone:string;customer_address1:string;customer_address2:string;customer_city:string;customer_region:string;customer_postal_code:string;customer_country:string;sales_total:number;generated_by_name:string;created_at:string;xlsx_storage_path:string|null;pdf_storage_path:string|null};
type PurchaseLine={purchase_order_id:string;line_number:number;product_description:string;quantity:number;unit_cost:number;line_total:number};
type SalesLine={sales_order_id:string;line_number:number;product_description:string;quantity:number;unit_price:number;line_total:number};
type OrderRow={id:string;dbId:string;kind:"Purchase Order"|"Sales Order";number:string;deal:string;party:string;contact:string;email:string;phone:string;address:string;total:number;createdBy:string;createdAt:string;xlsx:string|null;pdf:string|null;lines:OrderPdfLine[];notes:string[]};
const safe=(value:string)=>value.replace(/[^a-zA-Z0-9._-]+/g,"-").replace(/^-+|-+$/g,"").slice(0,70)||"Order";
const address=(item:{address1:string;address2:string;city:string;region:string;postal_code:string;country:string})=>[item.address1,item.address2,[item.city,item.region,item.postal_code].filter(Boolean).join(", "),item.country].filter(Boolean).join(" | ");
const rounded=(value:number)=>Math.round((value+Number.EPSILON)*100)/100;

export default function OrderHistory(){
 const [session,setSession]=useState<PddSession|null>(null),[orders,setOrders]=useState<OrderRow[]>([]),[filter,setFilter]=useState<"all"|"sales"|"purchase">("all"),[message,setMessage]=useState("Loading stored orders…"),[updating,setUpdating]=useState(""),[deleting,setDeleting]=useState(""),[editing,setEditing]=useState<string|null>(null),[draftLines,setDraftLines]=useState<OrderPdfLine[]>([]);
 useEffect(()=>{const requested=new URLSearchParams(window.location.search).get("type");if(requested==="sales"||requested==="purchase")setFilter(requested);void(async()=>{const active=await currentPddSession();if(!active){window.location.replace("/employee-login");return}setSession(active);const headers={Authorization:`Bearer ${active.access_token}`},[poResponse,soResponse,purchaseLineResponse,salesLineResponse]=await Promise.all([
  pddAuthFetch("/rest/v1/pdd_purchase_orders?select=id,po_number,deal_number,vendor_total,margin_percent,source_type,source_file_name,generated_by_name,created_at,xlsx_storage_path,pdf_storage_path,vendor:pdd_vendors(company_name,contact_name,email,phone,address1,address2,city,region,postal_code,country)&order=created_at.desc",{headers}),
  pddAuthFetch("/rest/v1/pdd_sales_orders?select=id,so_number,deal_number,customer_company,customer_contact,customer_email,customer_phone,customer_address1,customer_address2,customer_city,customer_region,customer_postal_code,customer_country,sales_total,generated_by_name,created_at,xlsx_storage_path,pdf_storage_path&order=created_at.desc",{headers}),
  pddAuthFetch("/rest/v1/pdd_purchase_order_lines?select=purchase_order_id,line_number,product_description,quantity,unit_cost,line_total&order=line_number.asc",{headers}),
  pddAuthFetch("/rest/v1/pdd_sales_order_lines?select=sales_order_id,line_number,product_description,quantity,unit_price,line_total&order=line_number.asc",{headers})
 ]);if([poResponse,soResponse,purchaseLineResponse,salesLineResponse].some(response=>response.status===401)){clearPddSession();window.location.replace("/employee-login");return}if([poResponse,soResponse,purchaseLineResponse,salesLineResponse].some(response=>!response.ok)){setMessage("Stored orders or their saved line items could not be loaded.");return}
 const purchase=await poResponse.json() as PurchaseOrder[],sales=await soResponse.json() as SalesOrder[],purchaseLines=await purchaseLineResponse.json() as PurchaseLine[],salesLines=await salesLineResponse.json() as SalesLine[],purchaseByOrder=new Map<string,OrderPdfLine[]>(),salesByOrder=new Map<string,OrderPdfLine[]>();
 for(const line of purchaseLines){const lines=purchaseByOrder.get(line.purchase_order_id)||[];lines.push({line:line.line_number,description:line.product_description,quantity:Number(line.quantity),unitPrice:Number(line.unit_cost),total:Number(line.line_total)});purchaseByOrder.set(line.purchase_order_id,lines)}
 for(const line of salesLines){const lines=salesByOrder.get(line.sales_order_id)||[];lines.push({line:line.line_number,description:line.product_description,quantity:Number(line.quantity),unitPrice:Number(line.unit_price),total:Number(line.line_total)});salesByOrder.set(line.sales_order_id,lines)}
 const rows:OrderRow[]=[...purchase.map(order=>{const vendor=order.vendor;return{id:`po-${order.id}`,dbId:order.id,kind:"Purchase Order" as const,number:order.po_number,deal:order.deal_number,party:vendor?.company_name||"Vendor",contact:vendor?.contact_name||"",email:vendor?.email||"",phone:vendor?.phone||"",address:vendor?address(vendor):"",total:Number(order.vendor_total),createdBy:order.generated_by_name,createdAt:order.created_at,xlsx:order.xlsx_storage_path,pdf:order.pdf_storage_path,lines:purchaseByOrder.get(order.id)||[],notes:[order.source_type==="spreadsheet"?`Created from ${order.source_file_name||"an uploaded spreadsheet"}; ${Number(order.margin_percent||0).toFixed(4)}% was proportionally deducted from all line totals.`:`Profit margin of ${Number(order.margin_percent||0).toFixed(4)}% has already been removed from the customer offer.`]}}),...sales.map(order=>({id:`so-${order.id}`,dbId:order.id,kind:"Sales Order" as const,number:order.so_number,deal:order.deal_number,party:order.customer_company,contact:order.customer_contact||"",email:order.customer_email||"",phone:order.customer_phone||"",address:address({address1:order.customer_address1||"",address2:order.customer_address2||"",city:order.customer_city||"",region:order.customer_region||"",postal_code:order.customer_postal_code||"",country:order.customer_country||""}),total:Number(order.sales_total),createdBy:order.generated_by_name,createdAt:order.created_at,xlsx:order.xlsx_storage_path,pdf:order.pdf_storage_path,lines:salesByOrder.get(order.id)||[],notes:["Customer-facing sales order. Vendor identity, vendor cost and internal margin are excluded."]}))].sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime());setOrders(rows);setMessage(rows.length?"":"No purchase orders or sales orders have been stored yet.")})()},[]);
 async function download(path:string|null,order:OrderRow,extension:"xlsx"|"pdf"){if(!session||!path)return;setMessage(`Preparing ${order.number} ${extension.toUpperCase()}…`);try{await downloadPddDocument(path,session,`${order.number}-${safe(order.party)}.${extension}`);setMessage("")}catch(error){setMessage(error instanceof Error?error.message:"The document could not be downloaded.")}}
 function beginEdit(order:OrderRow){setEditing(order.id);setDraftLines(order.lines.map(line=>({...line})));setMessage("")}
 function changeLine(index:number,key:"description"|"quantity"|"unitPrice",value:string){setDraftLines(current=>current.map((line,i)=>{if(i!==index)return line;const next={...line,[key]:key==="description"?value:Number(value)};return{...next,total:rounded(Number(next.quantity)*Number(next.unitPrice))}}))}
 async function saveOrder(order:OrderRow){if(!session||!draftLines.length)return;const clean=draftLines.map(line=>({...line,description:String(line.description).trim(),quantity:Number(line.quantity),unitPrice:Number(line.unitPrice),total:rounded(Number(line.quantity)*Number(line.unitPrice))}));if(clean.some(line=>!line.description||!Number.isFinite(line.quantity)||line.quantity<0||!Number.isFinite(line.unitPrice)||line.unitPrice<0)){setMessage("Enter a description and valid non-negative quantity and price on every line.");return}const total=rounded(clean.reduce((sum,line)=>sum+line.total,0));setUpdating(order.id);setMessage(`Saving ${order.number}…`);try{const isPo=order.kind==="Purchase Order",orderTable=isPo?"pdd_purchase_orders":"pdd_sales_orders",lineTable=isPo?"pdd_purchase_order_lines":"pdd_sales_order_lines",orderKey=isPo?"purchase_order_id":"sales_order_id",priceKey=isPo?"unit_cost":"unit_price",totalKey=isPo?"vendor_total":"sales_total",headers={Authorization:`Bearer ${session.access_token}`};const orderResponse=await pddAuthFetch(`/rest/v1/${orderTable}?id=eq.${order.dbId}`,{method:"PATCH",headers,body:JSON.stringify({[totalKey]:total,updated_at:new Date().toISOString()})});if(!orderResponse.ok)throw new Error("The order total could not be saved.");const lineRows=clean.map(line=>({[orderKey]:order.dbId,line_number:Number(line.line),product_description:line.description,quantity:line.quantity,[priceKey]:line.unitPrice,line_total:line.total})),lineResponse=await pddAuthFetch(`/rest/v1/${lineTable}?on_conflict=${orderKey},line_number`,{method:"POST",headers:{...headers,Prefer:"resolution=merge-duplicates,return=minimal"},body:JSON.stringify(lineRows)});if(!lineResponse.ok)throw new Error("The edited line items could not be saved.");const data={kind:isPo?"PURCHASE ORDER" as const:"SALES ORDER" as const,orderNumber:order.number,dealNumber:order.deal,partyLabel:isPo?"Vendor" as const:"Customer" as const,partyName:order.party,contact:order.contact,email:order.email,phone:order.phone,address:order.address,generatedBy:order.createdBy,lines:clean,total,notes:order.notes,fileBase:`${order.number}-${safe(order.party)}`},pdf=createOrderPdfBlob(data),xlsx=createOrderSpreadsheetBlob(data);if(order.pdf){const response=await replacePddDocument(order.pdf,pdf,session);if(!response.ok)throw new Error("The edited PDF could not be replaced.")}if(order.xlsx){const response=await replacePddDocument(order.xlsx,xlsx,session);if(!response.ok)throw new Error("The edited Excel file could not be replaced.")}setOrders(current=>current.map(item=>item.id===order.id?{...item,lines:clean,total}:item));setEditing(null);setMessage(`${order.number} and its documents were updated successfully.`)}catch(error){setMessage(error instanceof Error?error.message:"The order could not be updated.")}finally{setUpdating("")}}
 async function deletePurchaseOrder(order:OrderRow){
  if(!session||order.kind!=="Purchase Order"||!window.confirm(`Delete ${order.number}? This permanently removes the purchase order, all line items, and its saved Excel and PDF files.`))return;
  setDeleting(order.id);setMessage(`Deleting ${order.number}…`);setEditing(null);
  try{
   const response=await pddAuthFetch(`/rest/v1/pdd_purchase_orders?id=eq.${encodeURIComponent(order.dbId)}`,{method:"DELETE",headers:{Authorization:`Bearer ${session.access_token}`,Prefer:"return=representation"}});
   if(!response.ok)throw new Error("The purchase order could not be deleted.");
   const removed=await response.json() as {id:string}[];if(!removed.length)throw new Error("The purchase order was not deleted.");
   const documentResults=await Promise.all([order.xlsx,order.pdf].filter((path):path is string=>Boolean(path)).map(path=>deletePddDocument(path,session)));
   setOrders(current=>current.filter(item=>item.id!==order.id));
   setMessage(documentResults.some(result=>!result.ok&&result.status!==404)?`${order.number} was deleted, but one of its stored files could not be removed.`:`${order.number} was deleted.`);
  }catch(error){setMessage(error instanceof Error?error.message:"The purchase order could not be deleted.")}finally{setDeleting("")}
 }
 const visibleOrders=orders.filter(order=>filter==="all"||(filter==="sales"?order.kind==="Sales Order":order.kind==="Purchase Order")),heading=filter==="sales"?"Sales Order History":filter==="purchase"?"Purchase Order History":"Order History";
 return <main className="orderHistoryPage"><header><div><p className="eyebrow">DEAL WORKBOOK</p><h1>{heading}</h1><p>Review, edit and download stored {filter==="all"?"purchase orders and sales orders":filter==="sales"?"customer sales orders":"vendor purchase orders"}.</p></div><a className="button secondary" href="/employee">← Deal Workbook</a></header><nav className="orderTypeTabs" aria-label="Order type"><button className={filter==="all"?"active":""} onClick={()=>setFilter("all")}>All Orders</button><button className={filter==="sales"?"active":""} onClick={()=>setFilter("sales")}>Sales Orders</button><button className={filter==="purchase"?"active":""} onClick={()=>setFilter("purchase")}>Purchase Orders</button></nav>{message&&<p className="orderHistoryMessage">{message}</p>}<section className="orderHistoryList">{visibleOrders.map(order=><article key={order.id}><div><span>{order.kind}</span><h2>{order.number}</h2><p>{order.party} · Deal {order.deal}</p></div><div className="orderHistoryMeta"><small>Total</small><strong>{order.total.toLocaleString(undefined,{style:"currency",currency:"USD"})}</strong><small>Created by {order.createdBy||"Employee"} · {new Date(order.createdAt).toLocaleString()}</small></div><div className="orderDownloadActions"><button type="button" className="button secondary" disabled={!order.xlsx||(Boolean(updating)||Boolean(deleting))} onClick={()=>download(order.xlsx,order,"xlsx")}>Download Excel</button><button type="button" className="button secondary" disabled={!order.pdf||(Boolean(updating)||Boolean(deleting))} onClick={()=>download(order.pdf,order,"pdf")}>Download PDF</button><button type="button" className="button" disabled={!order.lines.length||(Boolean(updating)||Boolean(deleting))} onClick={()=>editing===order.id?setEditing(null):beginEdit(order)}>{editing===order.id?"Cancel Edit":"Edit Order"}</button>{order.kind==="Purchase Order"&&<button type="button" className="button orderDeleteButton" disabled={Boolean(updating)||Boolean(deleting)} onClick={()=>void deletePurchaseOrder(order)}>{deleting===order.id?"Deleting…":"Delete"}</button>}</div>{editing===order.id&&<div className="orderEditor"><p>Edit descriptions, quantities and the vendor cost or customer price. Line totals and the order total recalculate automatically.</p><div className="orderEditTable"><table><thead><tr><th>Line</th><th>Description</th><th>Qty</th><th>{order.kind==="Purchase Order"?"Unit Cost":"Unit Price"}</th><th>Total</th></tr></thead><tbody>{draftLines.map((line,index)=><tr key={`${line.line}-${index}`}><td>{line.line}</td><td><input value={line.description} onChange={event=>changeLine(index,"description",event.target.value)}/></td><td><input type="number" min="0" step="1" value={line.quantity} onChange={event=>changeLine(index,"quantity",event.target.value)}/></td><td><input type="number" min="0" step="0.01" value={line.unitPrice} onChange={event=>changeLine(index,"unitPrice",event.target.value)}/></td><td>{line.total.toLocaleString(undefined,{style:"currency",currency:"USD"})}</td></tr>)}</tbody><tfoot><tr><th colSpan={2}>TOTAL</th><th>{draftLines.reduce((sum,line)=>sum+Number(line.quantity||0),0).toLocaleString()}</th><th></th><th>{draftLines.reduce((sum,line)=>sum+Number(line.total||0),0).toLocaleString(undefined,{style:"currency",currency:"USD"})}</th></tr></tfoot></table></div><div className="orderEditorActions"><button className="button" disabled={updating===order.id} onClick={()=>void saveOrder(order)}>{updating===order.id?"Saving Changes…":"Save Order Changes"}</button><button className="button secondary" disabled={(Boolean(updating)||Boolean(deleting))} onClick={()=>setEditing(null)}>Cancel</button></div></div>}</article>)}</section></main>;
}
