"use client";
import {useEffect,useMemo,useState} from "react";
import {strToU8,unzipSync,zipSync} from "fflate";
import {clearPddSession,currentPddSession,pddAuthFetch,pddSupabaseKey,pddSupabaseUrl,uploadPddDocument,type PddSession} from "../../../lib/pdd-auth";
import {createOrderPdfBlob} from "../../../lib/orderPdf";
import "./purchase-order.css";

type Profile={email:string;display_name:string}; type AuthUser={id:string;email?:string};
type BidLine={lineNumber:number;quantity:number;unitBid:number}; type Bid={internal_bid_number:string;deal_number:string;company:string;total_bid:number;submitted_at:string;status:string;line_items?:BidLine[]};
type Source={row:number;quantity:number}; type Vendor={id:string;company_name:string;contact_name:string;email:string;phone:string;address1:string;address2:string;city:string;region:string;postal_code:string;country:string;created_by:string;created_by_name:string;created_by_email:string};
type UploadLine={line:number;quantity:number;values:Record<string,string>;sources:Source[]}; type Upload={id:string;original_name:string;storage_path:string;header_row:number;vendor_id:string;quantified_lines:UploadLine[];vendor:Vendor};
type Pricing={margin:number;target:number;mode:"margin"|"total"};
type PricedLine=BidLine&{unitPrice:number;lineTotal:number};

const col=(index:number)=>{let n=index+1,s="";while(n){n--;s=String.fromCharCode(65+n%26)+s;n=Math.floor(n/26)}return s};
const esc=(value:unknown)=>String(value??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");
const safe=(value:string)=>value.replace(/[^a-zA-Z0-9._-]+/g,"-").replace(/^-+|-+$/g,"").slice(0,70)||"Vendor";
const roundMoney=(value:number)=>Math.round((value+Number.EPSILON)*100)/100;
const productSku=(values:Record<string,string>)=>values.SKU||values["Part Number"]||values["Part #"]||values["Model Number"]||values.Model||"";
const productDescription=(values:Record<string,string>,line:number)=>Object.values(values).filter(Boolean).slice(0,8).join(" · ")||`Deal line ${line}`;
function pricedLines(bid:Bid,target:number):PricedLine[]{const lines=bid.line_items||[],weights=lines.map(line=>Math.max(0,line.unitBid*line.quantity)),weightTotal=weights.reduce((sum,value)=>sum+value,0);if(!lines.length||weightTotal<=0)return[];const targetCents=Math.round(target*100),raw=weights.map(weight=>targetCents*weight/weightTotal),cents=raw.map(Math.floor);let remaining=targetCents-cents.reduce((sum,value)=>sum+value,0);const order=raw.map((value,index)=>({index,remainder:value-Math.floor(value)})).sort((a,b)=>b.remainder-a.remainder);for(let i=0;i<remaining;i++)cents[order[i%order.length].index]++;return lines.map((line,index)=>{const lineTotal=cents[index]/100;return{...line,lineTotal,unitPrice:line.quantity?lineTotal/line.quantity:0}})}
function addCell(doc:Document,row:Element,ref:string,value:string|number,style?:number){const cell=doc.createElementNS("http://schemas.openxmlformats.org/spreadsheetml/2006/main","c");cell.setAttribute("r",ref);if(style!==undefined)cell.setAttribute("s",String(style));if(typeof value==="number"){const v=doc.createElementNS(cell.namespaceURI,"v");v.textContent=String(Math.round(value*100)/100);cell.append(v)}else{cell.setAttribute("t","inlineStr");const is=doc.createElementNS(cell.namespaceURI,"is"),t=doc.createElementNS(cell.namespaceURI,"t");t.textContent=value;is.append(t);cell.append(is)}row.append(cell)}
function addFormulaCell(doc:Document,row:Element,ref:string,formula:string,cached:number,style?:number){const cell=doc.createElementNS("http://schemas.openxmlformats.org/spreadsheetml/2006/main","c");cell.setAttribute("r",ref);if(style!==undefined)cell.setAttribute("s",String(style));const f=doc.createElementNS(cell.namespaceURI,"f"),v=doc.createElementNS(cell.namespaceURI,"v");f.textContent=formula;v.textContent=String(Math.round(cached*100)/100);cell.append(f,v);row.append(cell)}
function addCurrencyStyle(files:ReturnType<typeof unzipSync>){const path="xl/styles.xml",decoder=new TextDecoder();if(!files[path])return 0;const doc=new DOMParser().parseFromString(decoder.decode(files[path]),"application/xml"),root=doc.documentElement;let numFmts=root.getElementsByTagNameNS("*","numFmts")[0];if(!numFmts){numFmts=doc.createElementNS(root.namespaceURI,"numFmts");numFmts.setAttribute("count","0");root.insertBefore(numFmts,root.firstChild)}const ids=[...numFmts.getElementsByTagNameNS("*","numFmt")].map(node=>Number(node.getAttribute("numFmtId"))||0),numFmtId=Math.max(176,...ids)+1,numFmt=doc.createElementNS(root.namespaceURI,"numFmt");numFmt.setAttribute("numFmtId",String(numFmtId));numFmt.setAttribute("formatCode",'$#,##0.00');numFmts.append(numFmt);numFmts.setAttribute("count",String(numFmts.children.length));const cellXfs=root.getElementsByTagNameNS("*","cellXfs")[0];if(!cellXfs)return 0;const style=cellXfs.children.length,xf=doc.createElementNS(root.namespaceURI,"xf");for(const [key,value] of Object.entries({numFmtId:String(numFmtId),fontId:"0",fillId:"0",borderId:"0",xfId:"0",applyNumberFormat:"1"}))xf.setAttribute(key,value);cellXfs.append(xf);cellXfs.setAttribute("count",String(cellXfs.children.length));files[path]=strToU8(new XMLSerializer().serializeToString(doc));return style}

function addPurchaseOrderSheet(files:ReturnType<typeof unzipSync>,upload:Upload,bid:Bid,pricing:Pricing,poNumber:string,currencyStyle:number){
 const decoder=new TextDecoder(),workbookPath="xl/workbook.xml",relsPath="xl/_rels/workbook.xml.rels",typesPath="[Content_Types].xml";
 const workbook=new DOMParser().parseFromString(decoder.decode(files[workbookPath]),"application/xml"),sheets=workbook.getElementsByTagNameNS("*","sheets")[0],sheetNodes=[...sheets.getElementsByTagNameNS("*","sheet")],sheetNumber=sheetNodes.length+1;
 const rels=new DOMParser().parseFromString(decoder.decode(files[relsPath]),"application/xml"),relRoot=rels.documentElement,ids=[...relRoot.children].map(node=>Number((node.getAttribute("Id")||"").replace(/\D/g,""))||0),relId=`rId${Math.max(0,...ids)+1}`;
 const relationship=rels.createElementNS(relRoot.namespaceURI,"Relationship");relationship.setAttribute("Id",relId);relationship.setAttribute("Type","http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet");relationship.setAttribute("Target",`worksheets/sheet${sheetNumber}.xml`);relRoot.append(relationship);files[relsPath]=strToU8(new XMLSerializer().serializeToString(rels));
 const sheet=workbook.createElementNS(sheets.namespaceURI,"sheet");sheet.setAttribute("name","Purchase Order");sheet.setAttribute("sheetId",String(Math.max(0,...sheetNodes.map(node=>Number(node.getAttribute("sheetId"))||0))+1));sheet.setAttributeNS("http://schemas.openxmlformats.org/officeDocument/2006/relationships","r:id",relId);sheets.append(sheet);files[workbookPath]=strToU8(new XMLSerializer().serializeToString(workbook));
 const types=new DOMParser().parseFromString(decoder.decode(files[typesPath]),"application/xml"),typeRoot=types.documentElement,override=types.createElementNS(typeRoot.namespaceURI,"Override");override.setAttribute("PartName",`/xl/worksheets/sheet${sheetNumber}.xml`);override.setAttribute("ContentType","application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml");typeRoot.append(override);files[typesPath]=strToU8(new XMLSerializer().serializeToString(types));
 const vendor=upload.vendor,date=new Intl.DateTimeFormat("en-US",{year:"numeric",month:"long",day:"numeric"}).format(new Date()),address=[vendor.address1,vendor.address2,[vendor.city,vendor.region,vendor.postal_code].filter(Boolean).join(", "),vendor.country].filter(Boolean);
 const rows:Array<Array<string|number>>=[["PURCHASE ORDER"],["PO Number",poNumber,"Date",date],["Vendor",vendor.company_name],["Contact",vendor.contact_name],["Email",vendor.email],["Phone",vendor.phone],["Address",address.join(" · ")],[],["Deal",bid.deal_number],["PO Total",pricing.target],[],["Line","Description","Qty","Unit Price","Line Total"]];
 const byLine=new Map(upload.quantified_lines.map(line=>[line.line,line]));
 for(const line of pricedLines(bid,pricing.target)){const source=byLine.get(line.lineNumber),description=source?Object.values(source.values).filter(Boolean).slice(0,5).join(" · "):`Deal line ${line.lineNumber}`;rows.push([line.lineNumber,description,line.quantity,line.unitPrice,line.lineTotal])}
 rows.push([],["TOTAL","","","",pricing.target]);
 const firstLine=13,lastLine=12+pricedLines(bid,pricing.target).length,qtyTotal=pricedLines(bid,pricing.target).reduce((sum,line)=>sum+line.quantity,0);const xmlRows=rows.map((values,rowIndex)=>{const rowNumber=rowIndex+1;if(rowNumber===rows.length)return `<row r="${rowNumber}"><c r="A${rowNumber}" t="inlineStr"><is><t>TOTAL</t></is></c><c r="C${rowNumber}"><f>SUM(C${firstLine}:C${lastLine})</f><v>${qtyTotal}</v></c><c r="E${rowNumber}" s="${currencyStyle}"><f>SUM(E${firstLine}:E${lastLine})</f><v>${pricing.target}</v></c></row>`;return `<row r="${rowNumber}">${values.map((value,index)=>typeof value==="number"?`<c r="${col(index)}${rowNumber}"${index>=3?` s="${currencyStyle}"`:""}><v>${Math.round(value*100)/100}</v></c>`:`<c r="${col(index)}${rowNumber}" t="inlineStr"><is><t>${esc(value)}</t></is></c>`).join("")}</row>`}).join("");
 files[`xl/worksheets/sheet${sheetNumber}.xml`]=strToU8(`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><cols><col min="1" max="1" width="14" customWidth="1"/><col min="2" max="2" width="58" customWidth="1"/><col min="3" max="5" width="18" customWidth="1"/></cols><sheetData>${xmlRows}</sheetData></worksheet>`);
}

function createPurchaseOrderPdf(upload:Upload,bid:Bid,pricing:Pricing,poNumber:string,preparedBy:string){
 const vendor=upload.vendor,address=[vendor.address1,vendor.address2,[vendor.city,vendor.region,vendor.postal_code].filter(Boolean).join(", "),vendor.country].filter(Boolean),byLine=new Map(upload.quantified_lines.map(line=>[line.line,line]));
 const lines=pricedLines(bid,pricing.target).map(line=>{const source=byLine.get(line.lineNumber);return{line:line.lineNumber,description:source?Object.values(source.values).filter(Boolean).slice(0,5).join(" - "):`Deal line ${line.lineNumber}`,quantity:line.quantity,unitPrice:line.unitPrice,total:line.lineTotal}});
 return createOrderPdfBlob({kind:"PURCHASE ORDER",orderNumber:poNumber,dealNumber:bid.deal_number,partyLabel:"Vendor",partyName:vendor.company_name,contact:vendor.contact_name,email:vendor.email,phone:vendor.phone,address:address.join(" | "),generatedBy:preparedBy,lines,total:pricing.target,notes:[pricing.mode==="total"?`Unit prices were proportionally adjusted to the exact PO total of ${pricing.target.toLocaleString(undefined,{style:"currency",currency:"USD"})}.`:`Profit margin of ${pricing.margin.toFixed(4)}% has already been removed from the customer offer.`],fileBase:`${poNumber}-${safe(vendor.company_name)}`});
}

function downloadBlob(blob:Blob,filename:string){const link=document.createElement("a");link.href=URL.createObjectURL(blob);link.download=filename;link.click();setTimeout(()=>URL.revokeObjectURL(link.href),1000)}

async function generatePurchaseOrder(file:Blob,upload:Upload,bid:Bid,pricing:Pricing,poNumber:string){
 const files=unzipSync(new Uint8Array(await file.arrayBuffer())),currencyStyle=addCurrencyStyle(files),sheetPath=Object.keys(files).filter(path=>/^xl\/worksheets\/sheet\d+\.xml$/i.test(path)).sort()[0];if(!sheetPath)throw new Error("The original workbook does not contain a worksheet.");
 const decoder=new TextDecoder(),doc=new DOMParser().parseFromString(decoder.decode(files[sheetPath]),"application/xml"),sheetData=doc.getElementsByTagNameNS("*","sheetData")[0],rows=[...sheetData.getElementsByTagNameNS("*","row")],maxExisting=Math.max(...rows.flatMap(row=>[...row.getElementsByTagNameNS("*","c")].map(cell=>{const letters=(cell.getAttribute("r")||"").match(/^[A-Z]+/)?.[0]||"A";let n=0;for(const letter of letters)n=n*26+letter.charCodeAt(0)-64;return n})),1),start=maxExisting,headerRow=Number(upload.header_row||1);let header=rows.find(row=>Number(row.getAttribute("r"))===headerRow);if(!header){header=doc.createElementNS(sheetData.namespaceURI,"row");header.setAttribute("r",String(headerRow));sheetData.prepend(header)}addCell(doc,header,`${col(start)}${headerRow}`,"Unit Bid");addCell(doc,header,`${col(start+1)}${headerRow}`,"Total");const byLine=new Map(upload.quantified_lines.map(line=>[line.line,line.sources]));for(const line of pricedLines(bid,pricing.target)){const sources=byLine.get(line.lineNumber)||[],sourceQty=sources.reduce((sum,source)=>sum+source.quantity,0);let assignedCents=0;for(let index=0;index<sources.length;index++){const source=sources[index],row=rows.find(item=>Number(item.getAttribute("r"))===source.row);if(!row)continue;const cents=index===sources.length-1?Math.round(line.lineTotal*100)-assignedCents:Math.round(line.lineTotal*100*source.quantity/sourceQty);assignedCents+=cents;const total=cents/100,adjusted=source.quantity?total/source.quantity:0;addCell(doc,row,`${col(start)}${source.row}`,adjusted,currencyStyle);addCell(doc,row,`${col(start+1)}${source.row}`,total,currencyStyle)}}const last=Math.max(...rows.map(row=>Number(row.getAttribute("r"))||0))+2,totalRow=doc.createElementNS(sheetData.namespaceURI,"row");totalRow.setAttribute("r",String(last));addCell(doc,totalRow,`${col(start)}${last}`,"GRAND TOTAL");addFormulaCell(doc,totalRow,`${col(start+1)}${last}`,`SUM(${col(start+1)}${headerRow+1}:${col(start+1)}${last-2})`,pricing.target,currencyStyle);sheetData.append(totalRow);files[sheetPath]=strToU8(new XMLSerializer().serializeToString(doc));addPurchaseOrderSheet(files,upload,bid,pricing,poNumber,currencyStyle);const zipped=zipSync(files,{level:6}),bytes=zipped.buffer.slice(zipped.byteOffset,zipped.byteOffset+zipped.byteLength) as ArrayBuffer;return{grand:pricing.target,excel:new Blob([bytes],{type:"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"})};
}

export default function PurchaseOrderGenerator(){
 const [session,setSession]=useState<PddSession|null>(null),[user,setUser]=useState<AuthUser|null>(null),[profile,setProfile]=useState<Profile|null>(null),[bids,setBids]=useState<Bid[]>([]),[selected,setSelected]=useState(""),[pricingMode,setPricingMode]=useState<"margin"|"total">("margin"),[margin,setMargin]=useState("20"),[poTotal,setPoTotal]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false),[vendor,setVendor]=useState<Vendor|null>(null),[generated,setGenerated]=useState<{poNumber:string;vendor:Vendor;excel:Blob;pdf:Blob}|null>(null);
 useEffect(()=>{(async()=>{const active=await currentPddSession();if(!active){window.location.replace("/employee-login");return}setSession(active);const [bidResponse,userResponse,profileResponse]=await Promise.all([fetch("/api/admin/bids",{headers:{Authorization:`Bearer ${active.access_token}`}}),pddAuthFetch("/auth/v1/user",{headers:{Authorization:`Bearer ${active.access_token}`}}),pddAuthFetch("/rest/v1/pdd_employee_access?select=email,display_name",{headers:{Authorization:`Bearer ${active.access_token}`}})]);if(bidResponse.status===401||!userResponse.ok||!profileResponse.ok){clearPddSession();window.location.replace("/employee-login");return}const data=await bidResponse.json(),requested=new URLSearchParams(window.location.search).get("bid"),all=data.bids||[];setBids(all);setSelected(all.some((bid:Bid)=>bid.internal_bid_number===requested)?requested:all.find((bid:Bid)=>bid.status==="won")?.internal_bid_number||all[0]?.internal_bid_number||"");setUser(await userResponse.json() as AuthUser);setProfile((await profileResponse.json() as Profile[])[0]||null)})()},[]);
 const chosen=useMemo(()=>bids.find(bid=>bid.internal_bid_number===selected),[bids,selected]);
 const customerTotal=Number(chosen?.total_bid||0),enteredTotal=Number(poTotal),calculatedTarget=pricingMode==="total"?enteredTotal:roundMoney(customerTotal*(1-(Number(margin)||0)/100)),calculatedMargin=customerTotal>0?(1-calculatedTarget/customerTotal)*100:0;
 useEffect(()=>{setVendor(null);if(!session||!chosen)return;void(async()=>{const response=await pddAuthFetch(`/rest/v1/pdd_deal_uploads?select=vendor:pdd_vendors(id,company_name,contact_name,email,phone,address1,address2,city,region,postal_code,country,created_by,created_by_name,created_by_email)&deal_number=eq.${encodeURIComponent(chosen.deal_number)}&limit=1`,{headers:{Authorization:`Bearer ${session.access_token}`}});if(response.ok){const rows=await response.json() as {vendor:Vendor|null}[];setVendor(rows[0]?.vendor||null)}})()},[session,chosen]);
 async function generate(){if(!session||!selected||!user||!profile||!chosen)return;const target=calculatedTarget,percent=calculatedMargin;if(!Number.isFinite(target)||target<=0||target>Number(chosen.total_bid)){setMessage("Enter a PO total greater than $0 and no higher than the customer offer.");return}if(!Number.isFinite(percent)||percent<0||percent>=100){setMessage("Enter a margin from 0 through 99.99%.");return}if(chosen.status!=="won"){setMessage("Mark this customer offer as Won in Manage Deals before creating the purchase order.");return}setBusy(true);setGenerated(null);setMessage("");try{const bidResponse=await fetch(`/api/admin/bids?bid=${encodeURIComponent(selected)}`,{headers:{Authorization:`Bearer ${session.access_token}`}}),bidData=await bidResponse.json();if(!bidResponse.ok)throw new Error(bidData.error||"Bid could not be opened.");const uploadResponse=await pddAuthFetch(`/rest/v1/pdd_deal_uploads?select=id,original_name,storage_path,header_row,vendor_id,quantified_lines,vendor:pdd_vendors(id,company_name,contact_name,email,phone,address1,address2,city,region,postal_code,country,created_by,created_by_name,created_by_email)&deal_number=eq.${encodeURIComponent(bidData.bid.deal_number)}&limit=1`,{headers:{Authorization:`Bearer ${session.access_token}`}}),uploads=await uploadResponse.json() as Upload[];if(!uploads[0])throw new Error("The original spreadsheet mapping was not found.");const upload=uploads[0];if(!upload.vendor_id||!upload.vendor)throw new Error("This older deal does not have a vendor attached. Add its vendor before creating a purchase order.");if(!upload.original_name.toLowerCase().endsWith(".xlsx"))throw new Error("Purchase orders currently require the original upload to be an .xlsx workbook.");const pricing:Pricing={margin:percent,target:roundMoney(target),mode:pricingMode},poNumber=`PO-${bidData.bid.deal_number}`.toUpperCase(),record={po_number:poNumber,deal_number:bidData.bid.deal_number,internal_bid_number:bidData.bid.internal_bid_number,source_upload_id:upload.id,vendor_id:upload.vendor_id,margin_percent:pricing.margin,customer_total:Number(bidData.bid.total_bid),vendor_total:pricing.target,purchasing_owner_id:upload.vendor.created_by||null,purchasing_owner_name:upload.vendor.created_by_name||"Unassigned",purchasing_owner_email:upload.vendor.created_by_email||"",generated_by:user.id,generated_by_name:profile.display_name,generated_by_email:profile.email,updated_at:new Date().toISOString()};const recordResponse=await pddAuthFetch("/rest/v1/pdd_purchase_orders?on_conflict=deal_number",{method:"POST",headers:{Authorization:`Bearer ${session.access_token}`,Prefer:"resolution=merge-duplicates,return=representation"},body:JSON.stringify(record)});if(!recordResponse.ok)throw new Error("The purchase-order record could not be saved.");const storedOrders=await recordResponse.json() as {id:string}[],purchaseOrderId=storedOrders[0]?.id;if(!purchaseOrderId)throw new Error("The stored purchase order could not be opened.");const byLine=new Map(upload.quantified_lines.map(line=>[line.line,line])),lineRows=pricedLines(bidData.bid,pricing.target).map(line=>{const source=byLine.get(line.lineNumber),values=source?.values||{};return{purchase_order_id:purchaseOrderId,line_number:line.lineNumber,product_sku:productSku(values),product_description:productDescription(values,line.lineNumber),quantity:line.quantity,unit_cost:line.unitPrice,line_total:line.lineTotal}});if(lineRows.length){const linesSaved=await pddAuthFetch("/rest/v1/pdd_purchase_order_lines?on_conflict=purchase_order_id,line_number",{method:"POST",headers:{Authorization:`Bearer ${session.access_token}`,Prefer:"resolution=merge-duplicates,return=minimal"},body:JSON.stringify(lineRows)});if(!linesSaved.ok)throw new Error("The purchase order was saved, but its product lines could not be stored.")}const fileResponse=await fetch(`${pddSupabaseUrl}/storage/v1/object/authenticated/pdd-deal-uploads/${upload.storage_path.split("/").map(encodeURIComponent).join("/")}`,{headers:{apikey:pddSupabaseKey,Authorization:`Bearer ${session.access_token}`}});if(!fileResponse.ok)throw new Error("The original spreadsheet could not be downloaded.");const built=await generatePurchaseOrder(await fileResponse.blob(),upload,bidData.bid,pricing,poNumber),pdf=createPurchaseOrderPdf(upload,bidData.bid,pricing,poNumber,profile.display_name),base=`${user.id}/orders/purchase-orders/${poNumber}-${Date.now()}`,xlsxPath=`${base}.xlsx`,pdfPath=`${base}.pdf`;const [xlsxUpload,pdfUpload]=await Promise.all([uploadPddDocument(xlsxPath,built.excel,session),uploadPddDocument(pdfPath,pdf,session)]);if(!xlsxUpload.ok||!pdfUpload.ok)throw new Error("The purchase-order files could not be stored.");const pathResponse=await pddAuthFetch(`/rest/v1/pdd_purchase_orders?deal_number=eq.${encodeURIComponent(bidData.bid.deal_number)}`,{method:"PATCH",headers:{Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({xlsx_storage_path:xlsxPath,pdf_storage_path:pdfPath})});if(!pathResponse.ok)throw new Error("The stored purchase-order files could not be attached to the record.");setVendor(upload.vendor);setGenerated({poNumber,vendor:upload.vendor,excel:built.excel,pdf});setMessage(`${poNumber} was stored for ${upload.vendor.company_name} and credited to ${record.purchasing_owner_name}. Vendor total: ${built.grand.toLocaleString(undefined,{style:"currency",currency:"USD"})}.`)}catch(error){setMessage(error instanceof Error?error.message:"The purchase order could not be generated.")}finally{setBusy(false)}}
 return <main className="employeeDashboard reverseOfferPage"><header><div><p className="eyebrow">EMPLOYEE TOOL</p><h1>Purchase Order Generator</h1><p>Create a vendor PO from a winning offer or directly from a priced spreadsheet.</p></div><a className="button secondary" href="/employee">← Deal Workbook</a></header><section className="reverseOfferCard"><div className="poSourceTabs"><a className="active" href="/employee/reverse-offer">From Winning Offer</a><a href="/employee/purchase-order-upload">From Spreadsheet</a></div><label>Winning customer offer<select value={selected} onChange={event=>setSelected(event.target.value)}>{bids.map(bid=><option key={bid.internal_bid_number} value={bid.internal_bid_number}>{bid.status==="won"?"✓ WON · ":""}{bid.deal_number} · {bid.company} · {`$${Number(bid.total_bid).toLocaleString()}`}</option>)}</select></label>{chosen&&<p className={`poStatus ${chosen.status==="won"?"won":"pending"}`}>{chosen.status==="won"?"Winning offer confirmed":"This offer must be marked Won before a PO can be created"}</p>}{vendor&&<div className="poVendor"><span>VENDOR</span><strong>{vendor.company_name}</strong><small>{vendor.contact_name} · {[vendor.city,vendor.region].filter(Boolean).join(", ")}</small></div>}<div className="poPricing"><div className="poPricingTabs" role="group" aria-label="Purchase order pricing method"><button type="button" className={pricingMode==="margin"?"active":""} onClick={()=>setPricingMode("margin")}>Use profit margin</button><button type="button" className={pricingMode==="total"?"active":""} onClick={()=>{setPricingMode("total");if(!poTotal&&chosen)setPoTotal(roundMoney(Number(chosen.total_bid)*(1-(Number(margin)||0)/100)).toFixed(2))}}>Enter exact PO total</button></div>{pricingMode==="margin"?<label>Required profit margin<div className="marginInput"><input type="number" min="0" max="99.99" step="0.01" value={margin} onChange={event=>setMargin(event.target.value)}/><span>%</span></div></label>:<label>Exact purchase order total<div className="moneyInput"><span>$</span><input type="number" min="0.01" max={customerTotal||undefined} step="0.01" value={poTotal} onChange={event=>setPoTotal(event.target.value)} placeholder="7000.00"/></div><small>The same proportional discount is applied across all winning unit prices.</small></label>}</div>{chosen&&<div className="marginPreview"><span>Customer offer total</span><strong>{customerTotal.toLocaleString(undefined,{style:"currency",currency:"USD"})}</strong><span>{pricingMode==="total"?"Calculated discount":"Profit margin"}</span><strong>{Number.isFinite(calculatedMargin)?`${calculatedMargin.toFixed(4)}%`:"—"}</strong><span>Purchase order total</span><strong>{Number.isFinite(calculatedTarget)&&calculatedTarget>0?calculatedTarget.toLocaleString(undefined,{style:"currency",currency:"USD"}):"—"}</strong></div>}<div className="poIncludes"><b>The Excel workbook and PDF include:</b><span>Purchase Order with vendor contact, PO number and totals</span><span>Vendor&apos;s original spreadsheet with margin-adjusted Unit Bid and Total</span></div><button className="button" onClick={generate} disabled={busy||!selected||chosen?.status!=="won"}>{busy?"Creating purchase order…":"Generate Purchase Order"}</button>{generated&&<div className="orderDownloadActions"><button type="button" className="button secondary" onClick={()=>downloadBlob(generated.excel,`${generated.poNumber}-${safe(generated.vendor.company_name)}.xlsx`)}>Download Excel</button><button type="button" className="button secondary" onClick={()=>downloadBlob(generated.pdf,`${generated.poNumber}-${safe(generated.vendor.company_name)}.pdf`)}>Download PDF</button></div>}{message&&<p className="employeeAuthMessage">{message}</p>}</section></main>
}
