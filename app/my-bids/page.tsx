"use client";
import {useEffect,useState} from "react";
import {clearCustomerSession,currentCustomerSession} from "../../lib/customer-auth";
type Bid={internal_bid_number:string;deal_number:string;line_count:number;total_quantity:number;total_bid:number;status:string;submitted_at:string};
export default function MyBids(){const [bids,setBids]=useState<Bid[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const session=await currentCustomerSession();if(!session){window.location.replace("/customer-login");return}const response=await fetch("/api/customer/bids",{headers:{Authorization:`Bearer ${session.access_token}`}});if(response.status===401){clearCustomerSession();window.location.replace("/customer-login");return}const data=await response.json();setBids(data.bids||[]);setLoading(false)})()},[]);return <main className="customerBidsPage"><header><div><p className="eyebrow">CUSTOMER ACCOUNT</p><h1>My bids</h1><p>Track every account bid and its current result.</p></div><nav><a href="/employee">Deal Workbook</a><a href="/public-deal-desk">Browse open deals</a><button onClick={()=>{clearCustomerSession();window.location.assign("/customer-login")}}>Sign out</button></nav></header>{loading?<p>Loading your bids…</p>:bids.length?<div className="customerBidList">{bids.map(bid=><a className="customerBidLink" href={`/my-bids/${encodeURIComponent(bid.internal_bid_number)}`} key={bid.internal_bid_number}><article><div><span>{bid.deal_number}</span><h2>{bid.internal_bid_number}</h2><p>{new Date(bid.submitted_at).toLocaleString()} · {bid.line_count} lines · {bid.total_quantity} units</p></div><strong>${Number(bid.total_bid).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</strong><b className={`bidStatus ${bid.status}`}>{bid.status==="submitted"?"Pending":bid.status}</b></article></a>)}</div>:<section className="customerEmpty"><h2>No account bids yet</h2><p>Sign in before submitting a bid and it will appear here.</p><a className="button" href="/public-deal-desk">View open deals</a></section>}</main>}
